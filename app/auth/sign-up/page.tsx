'use client'
import { FormEvent, useState } from 'react'
import { createClient } from '@/lib/supabase/client'
import Link from 'next/link'
import { useRouter } from 'next/navigation'

export default function SignUpPage() { const router=useRouter(); const [displayName,setDisplayName]=useState(''); const [email,setEmail]=useState(''); const [password,setPassword]=useState(''); const [message,setMessage]=useState(''); const [loading,setLoading]=useState(false); async function submit(e:FormEvent){e.preventDefault();setLoading(true);setMessage('');const supabase=createClient();const {data,error}=await supabase.auth.signUp({email:email.trim().toLowerCase(),password,options:{data:{display_name:displayName.trim()},emailRedirectTo:process.env.NEXT_PUBLIC_DEV_SUPABASE_REDIRECT_URL||`${window.location.origin}/auth/callback`}});setLoading(false);if(error){const text=error.message.toLowerCase();setMessage(text.includes('already')?'An account with this email may already exist. Try signing in or resend the confirmation email.':text.includes('password')?'Choose a stronger password (at least 6 characters).':text.includes('rate')?'Too many attempts. Please wait a little and try again.':'We could not create your account. Please check the email and try again.');return}if(data.session){router.replace('/');router.refresh();return}router.push(`/auth/sign-up-success?email=${encodeURIComponent(email.trim().toLowerCase())}`)} return <main className="min-h-screen bg-background px-5 py-8 text-foreground"><div className="mx-auto flex min-h-[calc(100vh-4rem)] max-w-lg items-center"><section className="glass-panel w-full p-7 sm:p-12"><p className="eyebrow">Begin gently</p><h1 className="mt-3 font-serif text-4xl text-emerald-50">Create your Siraj account</h1><p className="mt-3 text-sm leading-6 text-muted-foreground">Your personal prayer record will be private to you.</p><form onSubmit={submit} className="mt-9 flex flex-col gap-5"><label className="flex flex-col gap-2 text-sm">Name<input required value={displayName} onChange={e=>setDisplayName(e.target.value)} className="auth-input" placeholder="Yusuf" /></label><label className="flex flex-col gap-2 text-sm">Email<input required type="email" value={email} onChange={e=>setEmail(e.target.value)} className="auth-input" placeholder="you@example.com" /></label><label className="flex flex-col gap-2 text-sm">Password<input required minLength={6} type="password" value={password} onChange={e=>setPassword(e.target.value)} className="auth-input" placeholder="At least 6 characters" /></label>{message&&<p role="status" className="rounded-xl border border-gold/20 bg-gold/10 px-4 py-3 text-sm text-gold">{message}</p>}<button disabled={loading} className="primary-button">{loading?'Creating…':'Create account'}</button></form><p className="mt-8 text-center text-sm text-muted-foreground">Already have an account? <Link href="/auth/login" className="text-emerald-300">Sign in</Link></p></section></div></main> }
